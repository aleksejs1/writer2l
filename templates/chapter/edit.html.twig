{% extends 'base.html.twig' %}

{% block title %}Edit Chapter{% endblock %}
{% block pageTitle %}Edit {{ chapter.title ? 'chapter' }}{% endblock %}

{% block body %}
    <div class="card card-custom">
        <div class="card-body">
            <div class="tab-content">
                {{ include('chapter/_form.html.twig', {'button_label': 'Update'}) }}
                <a href="{{ path('project_show_chapter', {'project': project.id, 'chapter': chapter.id}) }}">back to list</a>
                {{ include('chapter/_delete_form.html.twig') }}
            </div>
        </div>
    </div>
{% endblock %}
